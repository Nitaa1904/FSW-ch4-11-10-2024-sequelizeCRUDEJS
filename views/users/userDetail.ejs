<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detail User</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #fff6f9;
        font-family: "Comic Sans MS", cursive;
      }

      .user-card {
        background-color: #ff85a1;
        color: white;
        border-radius: 15px;
        box-shadow: 0 4px 10px rgba(255, 75, 130, 0.3);
        padding: 20px;
      }

      .user-card img {
        width: 150px;
        height: 150px;
        border-radius: 50%;
        object-fit: cover;
        border: 4px solid white;
      }
    </style>
  </head>
  <body>
    <div class="container py-5">
      <div class="user-card text-center mx-auto">
        <img src="<%= user.photoProfile %>" alt="User Photo" />
        <h2 class="mt-3"><%= user.firstName %></h2>
        <p>Age: <%= user.age %></p>
        <p>Phone: <%= user.phoneNumber %></p>
        <a href="/users" class="btn btn-light mt-3">Back to Users</a>
      </div>
    </div>
  </body>
</html>
